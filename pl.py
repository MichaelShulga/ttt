# from matplotlib import pyplot as plt
# import numpy as np

# x = list(range(1000)) + list(range(100, 900)) + list(range(400, 600))
# counts, bins = np.histogram(x, density=1, bins=30)
# plt.stairs(counts, bins)
# # plt.show()
# print(len(bins), len(counts))

import time
import numpy as np
import matplotlib.pyplot as plt

# # plt.axis([0, 10, 0, 1])

# for i in range(10):
#     y = np.random.random()
#     plt.scatter(i, y)
#     time.sleep(1)


#     plt.show()

plt.plot([1,2,3])
plt.show()
